import 'package:flutter/material.dart';

class DonorProfile extends StatelessWidget {
  const DonorProfile({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Donor Profile')));
  }
}
